import { StyleSheet } from "react-native";

export const summaryStyles = StyleSheet.create({
  chartCard: {
    flex: 1,
    borderWidth: 0,
    borderRadius: 14,
    padding: 14,
    alignItems: "center",
    marginBottom: 12,
  },
});
